# Machine Learning Fundamentals

Welcome to the **Machine Learning Fundamentals** task for IOAI 2024. In this task, you'll be using fundamental concepts.

## 1. Linear Regression with LaTeX
The goal is to implement a simple linear regression model where the relationship between the independent variable $x$ and the dependent variable $y$ is given by:

$$
y = \beta_0 + \beta_1 x + \epsilon
$$

Here, $\epsilon$ represents the error term. Try optimizing the parameters using gradient descent.

## 2. Decision Trees

- Gini Impurity: A measure of node impurity.
- Entropy: Information gain relies on this formula:
  $$
  H(S) = - \sum_{i=1}^{c} p_i \log_2(p_i)
  $$

## Task

Write a python script that implements this:

```python
import numpy as np

def compute_entropy(y):
    # Your code here
    pass
```

Good luck and have fun!
